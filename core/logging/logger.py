"""
Модуль для настройки базового логгера приложения.

Создаёт логгер с именем текущего модуля и настраивает базовую конфигурацию
logging с уровнем INFO. Используется для единообразного вывода логов
в разных частях проекта.
"""

import logging

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)
